# Custom OpenOCD config for RP2040 debugging
# Fix hardware breakpoint issues

# Force hardware breakpoints
gdb_breakpoint_override hard

# RP2040 specific settings
$_TARGETNAME.0 configure -event gdb-attach {
    halt
}

$_TARGETNAME.0 configure -event gdb-detach {
    resume
}
